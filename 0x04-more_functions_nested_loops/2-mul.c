#include <stdio.h>

/**
 * mul - function checks for a digit (0 through).
 * @a: a - Variable
 * @b: b - Variable
 * Return: Always 0.
 */

int mul(int a, int b)
{
	int mul;

	mul =a * b;
	
	return (mul);
}
