#include "main.h"

/**
 * _abs - Function computes the absolute value of an integer
 * @n: Integer to verify
 *
 * Return: Absolute value of integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

