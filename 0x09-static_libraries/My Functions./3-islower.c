#include "main.h"

/**
 * _islower - Looks for lowercase character
 * @c: Character to look for
 *
 * Return: 1 if c is lowercase, otherwise 0
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
