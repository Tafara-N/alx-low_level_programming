#include "main.h"

/**
 * _abs - Function computes the absolute value of an integer.
 *
 * @n: The integer.
 *
 * Return: int
 *       : The absolute value of an integer.
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}

	return (-n);
}
