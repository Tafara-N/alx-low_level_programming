#include <stdio.h>
#include <stdlib.h>

/**
 * mul - Function to multiply two integer numbers
 * @x: First integer
 * @y: Second integer
 *
 *Return: Results of the multiplication
 */

int mul(int x, int y)
{
	return (x * y);
}
